# SEARCH ENGINE

Search engine that allows users to query a large corpus of data. 

Indexes data by converting to XML, parsing and refining the files and queries the search based on Google’s PageRank algorithm. 

User types in free text query and program returns top ten most relevant pages for given query. 
